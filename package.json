{
  "name": "agentic-hitl-poc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Proof-of-concept for a human-in-the-loop development workflow driven by the GitHub Copilot coding agent.",
  "scripts": {
    "start": "tsx src/index.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
